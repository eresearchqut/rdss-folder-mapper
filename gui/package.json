{
  "name": "rdss-folder-mapper-gui",
  "version": "1.5.2",
  "private": true,
  "description": "Desktop GUI for RDSS Folder Mapper — map your research storage folders to your Desktop with a single click.",
  "scripts": {
    "sidecar": "node copy-sidecar.js",
    "tauri": "tauri",
    "dev": "npm run sidecar && tauri dev",
    "build": "node copy-sidecar.js",
    "dist": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
